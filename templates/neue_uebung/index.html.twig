{# templates/exercise/create.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
    <h1>Übung erstellen</h1>
    {{ form_start(form) }}
    {{ form_row(form.name) }}
    <button type="submit" class="btn btn-primary">Übung erstellen</button>
    {{ form_end(form) }}
{% endblock %}
