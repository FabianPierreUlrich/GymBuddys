{% extends 'base.html.twig' %}

{% block title %}Create New Training Plan{% endblock %}

{% block body %}
    <h1>Create New Training Plan</h1>
    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.description) }}
        <button type="submit" class="btn btn-primary">Save</button>
    {{ form_end(form) }}
{% endblock %}